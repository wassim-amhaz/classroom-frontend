import "@testing-library/jest-dom/vitest";

// jsdom does not implement matchMedia; several components (e.g. ThemeProvider)
// rely on it to detect the user's preferred color scheme.
if (typeof window !== "undefined" && !window.matchMedia) {
  Object.defineProperty(window, "matchMedia", {
    writable: true,
    value: (query: string) => ({
      matches: false,
      media: query,
      onchange: null,
      addListener: () => {},
      removeListener: () => {},
      addEventListener: () => {},
      removeEventListener: () => {},
      dispatchEvent: () => false,
    }),
  });
}